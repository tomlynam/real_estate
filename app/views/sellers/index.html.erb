<h1>Sellers</h1>

<% if @sellers.any? %>
	<ul>
		<% @sellers.each do |seller| %>
			<li>
				<h2><%= link_to seller.name, seller_path(seller) %></h2>
				<% end %>
			</li>
		</ul>
<% else %>
	<h3>No sellers, please create one!</h3>
<% end %>


<%= link_to 'New Seller', new_seller_path %>